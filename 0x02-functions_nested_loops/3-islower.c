#include "main.h"
/**
*main - check for lowercase characters
*coded by c
*Return:1 & 0
*/
int _islower(int c);
{
char ch = 'a';

while (ch <= 'z')
{
printf("%c", ch);
c++;
}
{
if(if c is lowercase);
return (1);

else ();
return (0);
}
}
