#include "main.h"

/**
*main - prints 10 times the alphabet, in lowercase
*coded using c
*Return:1
*/
void print_alphabet_x10(void)
{
char = q;
for (ch = q; ch <= 10; ch++)
_putchar(ch);
_putchar('\n');
return (1);
}
