#include <stdio.h>
/*
* main - function that returns sum of all parameters
* @const: constant declaration
* @n: variable
* Return: sum
*/

int sum_them_all(const unsigned int n, ...)
{
int sum;
sum = sum_them_all(50, 60, 70);
if (n == 0)
{
return (0);
}
}
