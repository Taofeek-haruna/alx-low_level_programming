#include <stdio.h>
#include <time.h>
/* let make mr n */

/*betty style for main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 0)
	{
		printf("%d is positive\n", n);
	}
	else if (n == 0)
	{
		printf("%d is zero\n", n);
	}
	else if (n < 0)
	{
		printf("%di is negative\n", n);
	}
	return (0);
}
