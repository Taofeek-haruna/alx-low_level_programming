#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strcat - function that concatenates two strings
* @src: The source of strings
* @dest: The destnation of the strings
*Return: Pointer to the resulting string dest
*
*/

char *_strcat(char *dest, char *src)
{
char dest = "Hello";
char src = "World!";
while (dest = src)
{
*s = src;
}
return (dest);
}
